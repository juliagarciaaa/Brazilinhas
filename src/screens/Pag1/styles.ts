import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    brazilinhas : {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
    },

    bolinha: {
        flexDirection: "row",
        marginBottom: 20,
        alignItems: "center",
        justifyContent: "center",
        
        
    }

})

